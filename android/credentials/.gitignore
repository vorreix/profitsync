# Belt-and-suspenders: the root .gitignore already ignores google-services.json
# everywhere, but a nested rule here survives branch switches (the same reason
# the root native rules exist) and makes the intent local + obvious.
# Ignore EVERYTHING in this folder except the committed example + this file.
*
!.gitignore
!google.services.example.json
