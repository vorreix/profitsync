# Mirrors the `make pr` pre-commit gate: lint -> type check -> tests.
# (The Makefile's first "format" step is `eslint --fix`, which mutates files;
#  in CI we run plain `eslint .` as the equivalent check.)
name: PR

on:
  pull_request:
  push:
    branches: [main, dev]

jobs:
  checks:
    name: lint · typecheck · test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npm run typecheck

      - name: Tests
        run: npm run test:ci
